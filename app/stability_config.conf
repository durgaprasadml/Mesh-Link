// Compose Compiler Stability Configuration
// Treat standard library collections and domain models as stable to prevent unnecessary recomposition overhead and reduce compiler code generation.
java.util.*
kotlin.collections.*
kotlinx.coroutines.flow.*
com.meshlink.domain.model.*
