# Diagrams

This directory is reserved for static image assets (PNG, SVG) if required in the future.

Currently, all architecture and flow diagrams in Mesh Link V3.0 documentation are generated using embedded Mermaid syntax within the Markdown files (e.g., `architecture.md`, `routing.md`). This ensures diagrams are version-controlled alongside the text and easily editable without external tools.
